<ul class="{{ class }}">
    {% for breadcrumb in breadcrumbs %}
    <li class="{{ breadcrumb.class }}">
        {% if breadcrumb.url %}
            <a href="{{ breadcrumb.url|e('html_attr') }}">{{ breadcrumb.title|e }}</a>
        {% else %}
            {{ breadcrumb.title|e }}
        {% endif %}
    </li>
    {% endfor %}
</ul>