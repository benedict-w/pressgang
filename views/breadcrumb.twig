<ul class="{{ class }}">
    {% for breadcrumb in breadcrumbs %}
    <li class="{{ breadcrumb.class }}">
        {% if breadcrumb.url %}
            <a href="#">{{ breadcrumb.title }}</a>
        {% else %}
            {{ breadcrumb.title }}
        {% endif %}
    </li>
    {% endfor %}
</ul>