<!-- offcanvas menu -->
<div class="navbar">
    <button type="button" class="navbar-toggle" data-toggle="offcanvas" data-target="#navbar-offcanvas" data-canvas="body">
        <span class="sr-only">{{ __("Toggle Navigation", THEMENAME) }}</span>
        <span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </span>
    </button>
    {% include('brand.twig') %}
</div>
<!-- nav -->
<nav id="navbar-offcanvas" class="navmenu navmenu-default navmenu-fixed-right offcanvas" role="navigation">
    {% if function('function_exists', 'icl_get_languages') %}
        {% include 'navbar-language-selector.twig' with { languages: function('icl_get_languages') } %}
    {% endif %}
    {% include 'menu.twig' with { 'menu': menu_primary, 'class': 'nav navmenu-nav primary' } %}
</nav>